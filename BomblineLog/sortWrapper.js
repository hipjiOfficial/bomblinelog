function sortStats() {
  sortMultipleItemBlocksPreserveFormulas();
  updateCardAndSkinProbabilities();
}
// I wrote the sorting script before the probability script, so I use this to execute them at the same time
